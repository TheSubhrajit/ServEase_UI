export type Bookingtype = {
    endTime: string;
    startTime: string;
    startDate? : any;
    endDate? : any;
    bookingPreference? : string;
    morningSelection ? : any;
    eveningSelection ? : any;
    timeRange ? : any;
    duration ? : any;
    role : string;
}