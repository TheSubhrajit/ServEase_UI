export type Bookingtype = {
    startDate? : any;
    endDate? : any;
    bookingPreference? : string;
    morningSelection ? : any;
    eveningSelection ? : any;
    timeRange ? : any;
    duration ? : any;
    
}